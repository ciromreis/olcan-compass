[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://olcan-compass-api.onrender.com"
  NODE_VERSION = "18"

# SPA redirect — all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
