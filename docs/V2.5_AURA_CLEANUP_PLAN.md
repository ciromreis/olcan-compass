# App Compass v2.5 - Aura Consolidation Cleanup Plan

**Date:** April 8, 2026  
**Status:** Post-Codex Consolidation - Domain Cleanup Phase  
**Branch:** `feature/v2.5-core`

---

## ✅ **What Codex Already Fixed**

### Core Topology (Complete)
- ✅ `/aura` and `/aura/discover` are canonical routes
- ✅ `/companion` and `/companion/discover` are compatibility redirects
- ✅ Legacy wrappers reduced to explicit aliases in:
  - `companionStore.ts`
  - `realCompanionStore.ts`
  - `components/layout/Navigation.tsx`
- ✅ Aura uses real API contract in `auraStore.ts`
- ✅ Legacy client aligned in `api-client.ts`
- ✅ Canonical entry created in `aura/discover/page.tsx`
- ✅ Compatibility maintained in `companion/discover/page.tsx`
- ✅ Sitemap adjusted
- ✅ Residual copy cleaned in:
  - `EvolutionStageCard.tsx`
  - `canonicalCompanionStore.ts`
  - `stores/index.ts`
  - `components/aura/index.ts`

### Build Status
✅ `pnpm --filter @olcan/web-v2.5 build` passes

---

## 🎯 **Remaining Work: Domain-Based Cleanup**

Codex identified **427 'companion' references across 49 files** in non-core areas.

### Priority Domains for Cleanup

#### **Domain 1: Gamification & Achievements** (High Priority)
**Files:** 23 matches in `lib/achievements.ts`, plus stores

**Current State:**
```typescript
// achievements.ts
export type AchievementCategory = 
  | 'companion'  // ❌ Should be 'aura'
  | 'evolution'
  | 'care'
  | 'social'

// Achievement examples:
{
  id: 'first_companion',           // ❌ Should be 'first_aura'
  name: 'First Companion',         // ❌ Should be 'First Aura'
  description: 'Discovered and bonded with your first career companion',  // ❌
  category: 'companion',           // ❌
  requirement: { type: 'create_companion', target: 1 },  // ❌
}
```

**Required Changes:**
1. Rename achievement category `'companion'` → `'aura'`
2. Update achievement IDs:
   - `first_companion` → `first_aura`
   - `companion_namer` → `aura_namer`
   - `evolve_companion` → `evolve_aura`
   - `reach_level` (keep, but update descriptions)
3. Update all descriptions to use "aura" or "presença"
4. Update requirement types:
   - `create_companion` → `create_aura`
   - `name_companion` → `name_aura`
   - `evolve_companion` → `evolve_aura`

**Impact:** ~23 lines in achievements.ts + related stores

---

#### **Domain 2: Audio System** (Medium Priority)
**Files:** 35 matches in `stores/audioStore.ts`

**Current State:**
```typescript
// Sound categories
category: 'ui' | 'companion' | 'guild' | 'marketplace' | 'achievement' | 'ambient'
                  ❌ Should be 'aura'

// Sound definitions
{
  id: 'companion_happy',      // ❌ Should be 'aura_happy'
  name: 'Companion Happy',    // ❌ Should be 'Aura Happy'
  url: '/sounds/companion/happy.mp3',  // ❌ Path needs update
  category: 'companion',      // ❌
}

// Helper functions
export const playCompanionSound = (soundId: string) => {  // ❌
  useAudioStore.getState().playSound(soundId)
}

// Sound constants
COMPANION_HAPPY: 'companion_happy',    // ❌
COMPANION_FEED: 'companion_feed',      // ❌
COMPANION_PLAY: 'companion_play',      // ❌
COMPANION_TRAIN: 'companion_train',    // ❌
COMPANION_EVOLVE: 'companion_evolve',  // ❌
COMPANION_LEVEL_UP: 'companion_level_up',  // ❌
```

**Required Changes:**
1. Rename sound category `'companion'` → `'aura'`
2. Update all sound IDs:
   - `companion_*` → `aura_*`
3. Update sound URLs:
   - `/sounds/companion/` → `/sounds/aura/`
4. Rename helper function:
   - `playCompanionSound` → `playAuraSound`
5. Update all sound constants in `SOUNDS` object

**Impact:** ~35 lines in audioStore.ts + sound file paths

**Note:** Actual sound files in `/public/sounds/companion/` may need to be moved to `/public/sounds/aura/` or kept as-is if they're generic

---

#### **Domain 3: Inventory System** (Low Priority)
**Files:** 8 matches in `lib/inventory.ts`

**Current State:**
```typescript
// Item descriptions
description: 'Restores 20 energy to your companion'  // ❌
description: 'Helps your companion evolve faster'    // ❌
description: 'Contains useful items for new companions'  // ❌
description: 'Teaches your companion a new ability'  // ❌

// Function signatures
export function useItem(item: Item, companionId: string): {  // ❌ param name
  success: boolean
  message: string
  effect?: any
}
```

**Required Changes:**
1. Update item descriptions to use "aura" or "presença"
2. Rename function parameter:
   - `companionId` → `auraId`
3. Update any internal logic that references companion

**Impact:** ~8 lines in inventory.ts

---

#### **Domain 4: Stores (Legacy Wrappers)** (Medium Priority)
**Files:**
- `companionPersonalityStore.ts` (64 matches) - **Highest concentration**
- `eventDrivenGamificationStore.ts` (47 matches)
- `realtimeStore.ts` (32 matches)
- `gamificationStore.ts` (20 matches)
- `companionStore.ts` (5 matches) - Already aliased
- `realCompanionStore.ts` (6 matches) - Already aliased
- `canonicalCompanionStore.ts` (2 matches) - Already aliased

**Current State:**
Codex already reduced these to aliases, but they still contain companion terminology internally.

**Required Changes:**
1. **companionPersonalityStore.ts** - Rename to `auraPersonalityStore.ts`
   - Update all internal references
   - Update exports
   - Update imports in consuming files
2. **eventDrivenGamificationStore.ts** - Update companion event types
3. **realtimeStore.ts** - Update companion-related real-time events
4. **gamificationStore.ts** - Update companion-related gamification logic

**Impact:** ~169 lines across 4 stores

---

#### **Domain 5: Components** (Low Priority)
**Files:**
- `CompanionCustomization.tsx` (8 matches)
- `EvolutionCeremony.tsx` (5 matches) - Already partially cleaned
- `Leaderboard.tsx` (15 matches)
- `GamificationIntegration.tsx` (2 matches)

**Required Changes:**
1. Rename `CompanionCustomization.tsx` → `AuraCustomization.tsx`
2. Update all internal references in components
3. Update imports in parent components

**Impact:** ~30 lines across 4 components

---

#### **Domain 6: YouTube Integration** (Low Priority - Experimental)
**Files:**
- `app/youtube/page.tsx` (19 matches)
- `app/youtube/working-studio.tsx` (7 matches)

**Status:** These appear to be experimental/working files

**Recommendation:** Archive or clearly mark as experimental before cleanup

---

#### **Domain 7: Other Modules** (Low Priority)
**Files:**
- `app/guilds/working-page.tsx` (14 matches) - Working file
- `app/export/page.tsx` (9 matches)
- `app/analytics/page.tsx` (3 matches)
- `lib/quests.ts` (9 matches)
- `lib/guild.ts` (3 matches)
- `lib/leaderboard.ts` (2 matches)
- `lib/api.ts` (7 matches)
- `lib/api-client.ts` (9 matches) - Already aligned by Codex
- `types/companion.ts` (10 matches) - Type definitions
- `types/index.ts` (7 matches)

**Required Changes:**
1. Update type definitions in `types/companion.ts` → `types/aura.ts`
2. Update API client references
3. Update quest/guild/leaderboard logic
4. Update export functionality

**Impact:** ~60 lines across 11 files

---

## 📊 **Cleanup Summary by Priority**

| Priority | Domain | Files | Est. Lines | Complexity |
|----------|--------|-------|------------|------------|
| **High** | Gamification & Achievements | 3 | ~30 | Medium |
| **Medium** | Audio System | 1 | ~35 | Low |
| **Medium** | Legacy Store Wrappers | 4 | ~170 | High |
| **Low** | Inventory System | 1 | ~8 | Low |
| **Low** | Components | 4 | ~30 | Medium |
| **Low** | Type Definitions | 2 | ~20 | Low |
| **Low** | Other Modules | 11 | ~60 | Medium |
| **Archive** | YouTube (Experimental) | 2 | ~26 | N/A |
| **Total** | | **28** | **~379** | |

---

## 🎯 **Recommended Cleanup Order**

### **Phase 1: High-Impact, Low-Risk** (Do First)
1. ✅ **Gamification & Achievements** - Clear user-facing impact
   - Update `lib/achievements.ts`
   - Update achievement-related stores
   - Test achievement unlocking

2. ✅ **Audio System** - Clear, isolated changes
   - Update `stores/audioStore.ts`
   - Update sound constants
   - Test sound playback (or stub if files don't exist)

### **Phase 2: Type Safety** (Do Second)
3. ✅ **Type Definitions** - Foundation for other changes
   - Rename `types/companion.ts` → `types/aura.ts`
   - Update all type exports
   - Update imports across codebase

4. ✅ **Inventory System** - Depends on types
   - Update `lib/inventory.ts`
   - Update item descriptions
   - Update function signatures

### **Phase 3: Store Consolidation** (Do Third)
5. ✅ **Legacy Store Wrappers** - Complex but necessary
   - Rename `companionPersonalityStore.ts` → `auraPersonalityStore.ts`
   - Update `eventDrivenGamificationStore.ts`
   - Update `realtimeStore.ts`
   - Update `gamificationStore.ts`
   - Update all imports

### **Phase 4: UI Components** (Do Fourth)
6. ✅ **Components** - User-facing changes
   - Rename `CompanionCustomization.tsx` → `AuraCustomization.tsx`
   - Update component internals
   - Update parent imports

### **Phase 5: Integration Points** (Do Fifth)
7. ✅ **Other Modules** - API, quests, guilds, etc.
   - Update `lib/quests.ts`
   - Update `lib/guild.ts`
   - Update `lib/leaderboard.ts`
   - Update `app/export/page.tsx`
   - Update `app/analytics/page.tsx`

### **Phase 6: Cleanup** (Do Last)
8. ✅ **Archive Experimental** - Move to archive folder
   - Move `app/youtube/` to `_archive/youtube/`
   - Move `app/guilds/working-page.tsx` to `_archive/`
   - Add README explaining archived status

---

## ✅ **Success Criteria**

After cleanup, the codebase should have:

1. **Zero "companion" references** in active code paths
2. **Consistent "aura" terminology** across:
   - Routes (`/aura/*`)
   - Stores (`auraStore.ts`, `auraPersonalityStore.ts`)
   - Components (`AuraCustomization.tsx`)
   - Types (`types/aura.ts`)
   - API contracts
   - Sound system
   - Achievements
   - Inventory

3. **Clear separation** between:
   - **Active code** - Uses "aura" exclusively
   - **Compatibility layer** - `/companion/*` routes redirect to `/aura/*`
   - **Archived code** - Experimental features in `_archive/`

4. **Build passes** - `pnpm --filter @olcan/web-v2.5 build` succeeds
5. **No regressions** - All existing functionality works with new terminology

---

## 🚨 **Critical Constraints**

### **DO NOT TOUCH:**
- ❌ `apps/app-compass-v2/` - Stable v2 app
- ❌ `apps/site-marketing-v2.5/` - Marketing site (under separate revision)
- ❌ Any files outside `apps/app-compass-v2.5/`

### **MUST MAINTAIN:**
- ✅ Compatibility redirects in `/companion/*` routes
- ✅ Existing API contracts (unless explicitly updating)
- ✅ Database schema (companion → aura migration handled separately)
- ✅ Build stability

---

## 📝 **Implementation Notes**

### **Naming Conventions**
- **English (code):** `aura`, `AuraStore`, `useAura()`
- **Portuguese (UI):** "presença", "Sua presença", "Aura"
- **Files:** `auraStore.ts`, `AuraCustomization.tsx`, `types/aura.ts`
- **Routes:** `/aura`, `/aura/discover`, `/aura/achievements`

### **Backward Compatibility**
Keep these compatibility aliases until database migration:
```typescript
// In auraStore.ts
export { useAuraStore as useCompanionStore }  // Temporary alias
export type { Aura as Companion }              // Temporary type alias
```

### **Testing Strategy**
1. **Unit tests** - Update test descriptions and assertions
2. **Integration tests** - Verify aura creation, evolution, care activities
3. **E2E tests** - Test full user journey through `/aura` routes
4. **Manual testing** - Verify sound playback, achievements, UI components

---

## 🎁 **Expected Outcomes**

After completing this cleanup:

1. **Clearer architecture** - Single source of truth for "aura" concept
2. **Easier onboarding** - No confusion between "companion" and "aura"
3. **Better maintainability** - Consistent terminology reduces cognitive load
4. **Safer evolution** - Clear separation of concerns for future features
5. **Professional codebase** - No legacy terminology pollution

---

## 📅 **Estimated Effort**

- **Phase 1 (Achievements + Audio):** 1-2 hours
- **Phase 2 (Types + Inventory):** 1 hour
- **Phase 3 (Stores):** 3-4 hours (most complex)
- **Phase 4 (Components):** 1-2 hours
- **Phase 5 (Integration):** 2-3 hours
- **Phase 6 (Archive):** 30 minutes
- **Testing & Verification:** 2-3 hours

**Total:** 11-16 hours of focused work

---

## 🤖 **Agentic IDE Execution Strategy**

For an AI agent to execute this cleanup autonomously:

### **Prerequisites**
1. Verify build passes: `pnpm --filter @olcan/web-v2.5 build`
2. Create feature branch: `git checkout -b refactor/aura-terminology-cleanup`
3. Run tests baseline: `pnpm --filter @olcan/web-v2.5 test` (if tests exist)

### **Execution Pattern (Per Phase)**
```bash
# For each phase:
1. Read target files
2. Create multi_edit with all changes
3. Verify no syntax errors
4. Run build: pnpm --filter @olcan/web-v2.5 build
5. If build fails, revert and analyze
6. If build passes, commit with descriptive message
7. Move to next phase
```

### **Commit Strategy**
```
refactor(aura): update achievements terminology from companion to aura
refactor(aura): update audio system to use aura terminology
refactor(aura): rename companion types to aura types
refactor(aura): update inventory system descriptions
refactor(aura): rename companion stores to aura stores
refactor(aura): rename CompanionCustomization to AuraCustomization
refactor(aura): update integration modules (quests, guilds, leaderboard)
chore(archive): move experimental youtube and guilds features to archive
```

### **Rollback Plan**
If any phase breaks the build:
1. `git reset --hard HEAD~1` (revert last commit)
2. Analyze build errors
3. Fix specific issues
4. Retry phase

---

**Prepared by:** Cascade AI  
**For:** Olcan Compass v2.5 Aura Consolidation  
**Next Action:** Execute Phase 1 (Achievements + Audio) or await user approval
